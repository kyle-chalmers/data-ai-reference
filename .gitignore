# IDE files
.idea/
*.iml

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.env
.venv
pip-log.txt
pip-delete-this-directory.txt

# Jupyter Notebook
.ipynb_checkpoints

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Logs
*.log
logs/

# Database
*.db
*.sqlite3

# Temporary files
*.tmp
*.temp
*~

# Editor backup files
*~
*.swp
*.swo

# Planning artifacts (AI agent working files)
.planning/

# Claude Code specific
.claude/commands/.DS_Store

# Private Claude commands (not tracked)
.claude/commands/generate-script-outline.md

# Local notes (not tracked)
LOCAL_*.md
*_LOCAL.md
.local/

# Internal / non-public content (scripts, recording notes, private context)
.internal/

# Personal data (LinkedIn exports, etc.) — never committed to public repo
videos/az_tech_week_workshop/linkedin_data/

# Local tool/IDE config, not project source
.codex/
.project

# Per-machine agent tooling config (RuleMetric hooks, local permission grants).
# These hold absolute home-directory paths and personal MCP connections, so they
# must never land in this public repo. settings.local.json is also covered by a
# global gitignore on Kyle's machine; this repeats it so the guard travels with
# the repo rather than depending on one machine's config.
.agents/hooks.json
.claude/settings.local.json
.claude/settings.local.json.bak
.cursor/hooks.json

# Claude Code ship-gate review artifacts (machine-local)
.shipgate/
