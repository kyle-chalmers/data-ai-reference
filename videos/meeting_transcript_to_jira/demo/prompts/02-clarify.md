# 2 — Draft the clarifying questions

Run after the candidate table exists.

```
For each item marked clarify in the table above, draft a short message back to the
person who requested it.

Ask only for the fields that are actually missing. Keep each message under four
sentences. Quote the line from the transcript that prompted it so they have context.
Do not ask for anything the meeting already answered.
```

**Expected:** one short message per `clarify` item, each quoting the relevant transcript line.

**If the messages are generic** or ask for everything: tell it to re-read the Missing fields column
and ask only for those.
